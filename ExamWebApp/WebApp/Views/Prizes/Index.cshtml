@model IEnumerable<WebApp.ViewModels.PrizeDetailsDeleteViewModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Prize.PrizeName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RaffleResult)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Raffle)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Prize.PrizeName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RaffleResult)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Raffle)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Prize.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Prize.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Prize.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
