@model IEnumerable<WebApp.ViewModels.RaffleResultDetailsDeleteViewModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Raffle)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.User)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RaffleResult.AnonymousUserName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Prize)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Raffle)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.User)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RaffleResult.AnonymousUserName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Prize)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.RaffleResult.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.RaffleResult.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.RaffleResult.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
