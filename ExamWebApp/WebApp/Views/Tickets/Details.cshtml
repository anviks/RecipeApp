@model WebApp.ViewModels.TicketDetailsDeleteViewModel

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Ticket</h4>
    <hr />
    <partial name="_DetailsDelete" model="Model"/>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model?.Ticket.Id">Edit</a> |
    <a asp-action="Index">Back to List</a>
</div>
